import Footer from '../../components/Footer';
import { render } from '@testing-library/react';

describe ('Footer',  () => {
    test('renders Footer component', async () => {render(<Footer />);});
});

